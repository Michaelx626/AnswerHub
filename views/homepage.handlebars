    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">
          <i class="fab fa-icon"></i>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbar-collapse">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbar-collapse">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item">
              <a class="nav-link" href="/">
                <i class="fas fa-home"></i> Home
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link" id="profile-button" href="/profile">
                <i class="fas fa-user"></i> Profile
              </a>
            </li>
             <li class="nav-item">
    <a class="nav-link" href="#" data-bs-toggle="modal" data-bs-target="#exampleModal">
      <i class="fa-solid fa-magnifying-glass"></i> Search</a>
    </li>
    <li class="nav-item">
    <a class="nav-link logout-link" href="/logout" >
      <i class="fas fa-sign-out-alt"></i> Logout</a>
    </li>
           
          </ul>
        </div>
      </div>
    </nav>
    <!-- Content Area -->
    <div class="container mt-3">
      <div class="row">
        <!-- Left Column -->
        <div class="col-md-3">
          <div class="card">
            <div class="card-header">
              <h5>Profile</h5>
            </div>
            <div class="card-body">
              <img src='/uploads/{{users.profilePic}}' alt="Profile Picture" class="img-fluid rounded-circle mb-3">
              <h4>{{users.name}}</h4>
              <a class="text-muted" href="mailto:{{users.email}}">{{users.email}}</a>
              <ul class="list-unstyled">
                 <div class='profile-bio'>
          <p id="userBio">{{users.userBio}}</p>
        </div>
               <li><i class="fa-brands fa-bilibili"></i>AI Assistant</li>
              </ul>
            </div>
          </div>
         
        </div>
        <!-- Middle Column -->
        <div class="col-md-6">
          <div class="card mb-3"><div class="card-header">
              <h5>What's happening?</h5>
            </div>
            <div class="card-body">
              <form>
                <div class="form-group">
                  <textarea class="form-control" id="user_inputbox" placeholder="Write something..." rows="3"></textarea>
                </div>
                <button type="submit" class="btn btn-primary post_btn">Post</button>
              </form>
            </div>
          </div>

          {{#each posts as |post|}}
          <div class="card">
            <div class="card-body">
              <div class="media mb-3">
                <img src="https://via.placeholder.com/50" class="mr-3 rounded-circle" alt="User Picture">
                <div class="media-body">
                  <div class="post-grid">
                    <h5 class="mt-0 post_user">{{post.user.name}} </h5>
                    <p class="post_content"> {{post.content}}</p>
                    <p class="mt-0 post_date">{{format_date post.date_created}} </p>
                  </div>
                  <div class="mt-3">
                    <a href="#"><i class="far fa-comment"></i> 10</a>
                    <a href="#"><i class="far fa-heart"></i> 30</a>                
                  </div>
                </div>
              </div>
            </div>
          </div>
          {{/each}}

        <!-- Right Column -->
          <div class="card">
            <div class="card-header">
              <h5>Recommened Connections</h5>
            </div>
            <div class="card-body">
              <div class="media mb-3 recommended-user-block">
                <img src="https://via.placeholder.com/50" class="mr-3 rounded-circle recommended-user" alt="User Picture">
                <div class="media-body">
                  <h5 class="mt-0 recommended-name">Jane Smith </h5>
                  <button type="button" class="btn btn-primary follow-btn">Follow</button>
                </div>
              </div>
              <div class="media mb-3 recommended-user-block">
                <img src="https://via.placeholder.com/50" class="mr-3 rounded-circle recommended-user" alt="User Picture">
                <div class="media-body">
                  <h5 class="mt-0 recommended-name">John Doe</h5>
                  <button type="button" class="btn btn-primary follow-btn">Follow</button>
                </div>
              </div>
            </div>
          </div>
      </div>    
<!--Search Modal-->
<div
  class='modal fade'
  id='exampleModal'
  tabindex='-1'
  aria-labelledby='exampleModalLabel'
  aria-hidden='true'
>
  <div class='modal-dialog'>
    <div class='modal-content'>
      <div class='modal-header'>
        <h1 class='modal-title fs-5' id='exampleModalLabel'>Edit Profile</h1>
        <button
          type='button'
          class='btn-close'
          data-bs-dismiss='modal'
          aria-label='Close'
        ></button>
      </div>
      <div class='modal-body'>
        <div class='input-group mb-3'>
          <span class='input-group-text' id='basic-addon1'>@</span>
          <input
            type='text'
            class='form-control user_searches'
            placeholder='Username'
            aria-label='Username'
            aria-describedby='basic-addon1'
          />
        </div>
      </div>
      <div class='modal-footer'>
        <button
          type='button'
          class='btn btn-secondary'
          data-bs-dismiss='modal'
        >Close</button>
        <button type='button' class='btn btn-primary search_btn'>Search</button>
      </div>
    </div>
  </div>
</div>

<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/popper.js@1.12.9/dist/umd/popper.min.js" integrity="sha384-ApNbgh9B+Y1QKtv3Rn7W3mgPxhU9K/ScQsAP7hUibX39j7fakFPskvXusvfa0b4Q" crossorigin="anonymous"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.1.0/js/bootstrap.min.js"></script>
<script src="./js/homePage.js"></script>
<script src="./js/logout.js"></script>
<script src="./js/post.js"></script>
                

      
        {{#each posts as |post|}}
        <div class="card">
          <div class="card-body">
            <div class="media mb-3">
             <img 
               src="https://via.placeholder.com/50" 
               class="m3 rounded-circle align-self-center" 
               alt="{{post.user.name}} profile picture"
               width='50'
               height="50"
             />
             <div class="media-body">
              <div class="post-grid">
                <p class="mt-0 post_user">{{post.user.name}} </p>
                <p class="post_content"> {{post.content}}</p>
                <p class="mt-0 post_dater">{{format_date post.date_created}} </p>
              </div>
              <div class="col-md-8 p-2 m-1 align-self-center" id="contents">
                <p class="post_content"> {{post.content}}</p>
              </div>
              <div class="mt3">
                <a href="/post/{{post.id}}"><i class="far fa-comment"></i>
                {{#with post.comments}}
                {{post.comments.length}}
                {{/with}}
                </a>
                <a href="#" data-value="{{post.number_likes}}" data-id="{{post.id}}" class=getLikes><i class="far fa-heart"></i>{{post.number_likes}}</a>    
              </div>
            </div>
          </div>
        </div>
      </div>
    {{/each}}
  </div>
</div>

<script src="/js/likes.js"></script>
<script src="/js/post.js"></script>

                
